# -*- mode: org; coding: utf-8; -*-
#+TITLE: Modern Emacs Configuration Native Compilation 警告の収集・管理・診断 詳細設計書
#+AUTHOR: YAMASHITA, Takao
#+DATE: 2026-03 (rev.3)
#+LANGUAGE: ja
#+OPTIONS: toc:3 num:t ^:nil
#+STARTUP: overview

* 1. 概要
:PROPERTIES:
:CUSTOM_ID: overview
:END:

このドキュメントは =README.org= (Literate Programming ソース) から自動抽出した詳細設計書です。
Fix A–AD (9ラウンド評価) をすべて反映した最終版 rev.3 です。

| 項目 | 数値 |
|------|------|
| モジュール (ロード順) | 72 |
| タングル対象ファイル | 89 |
| 公開 defun | 93 |
| defcustom 変数 | 57 |
| フック登録 | 59 |

** 設計原則
:PROPERTIES:
:CUSTOM_ID: design-principles
:END:

| 原則 | 内容 |
|------|------|
| *決定論的起動* | =modules.el= の明示的ロード順、=condition-case= によるモジュール単位エラー隔離。起動失敗が上位レイヤーに波及しない。 |
| *レイヤー分離* | 依存は下位→上位方向のみ。core → ui → completion → orgx → dev の順。依存逆転禁止。 |
| *削除安全性* | =my:modules-skip= でモジュールをスキップ可能。=fboundp= / =boundp= ガードにより欠損モジュールで Emacs が起動不能にならない。 |
| *Literate Programming* | =README.org= が唯一の真実。=org-babel-tangle= → =lisp/<layer>/<module>.el=。手動編集不可。 |
| *前方互換性* | Emacs 30+ 対象・31+ 想定。=if-let*= / =when-let*= / =and-let*= 使用。obsolete API 禁止。 |
| *setopt 統一* | すべての =defcustom= 変数は =setopt= で設定。=leaf :custom= / =:pre-setq= を優先。=:config= 内でも原則 =setopt=。ただし backquote 展開・関数参照を含む runtime 評価値（=org-capture-templates= 等）は =setopt= の型チェッカーが誤検知するため =setq= を使用する。 |
| *命名規則* | 公開関数: =my/= or =<module>-=。内部ヘルパー: =<module>--= (double dash)。混在形 =my/<topic>--= は禁止。パス変数: =my:d:*=。 |
| *ドキュメント* | 公開 defun はすべてドキュメント文字列を持つ。=defcustom= を持つモジュールは =defgroup= を宣言し、=:group= に自モジュールのグループを指定。 |

** コーディングルール
:PROPERTIES:
:CUSTOM_ID: coding-rules
:END:

| ルール | 詳細 |
|--------|------|
| =lexical-binding: t= | 全モジュール必須 |
| =(provide 'FEATURE)= | タングルファイル名と一致すること |
| =Built-in は :straight nil= | Emacs 組み込みパッケージは =:straight nil= を宣言 |
| =leaf= 順序 | =:straight= → =:bind= → =:hook= → =:custom= → =:config= |
| =setopt for defcustom= | 全 =defcustom= 変数は原則 =setopt= / =leaf :custom= で設定。=setq= は =defvar=・内部状態・一時オーバーライドのみ。例外: backquote 展開や関数参照を含む runtime 評価値は =setopt= の型チェッカーが誤検知するため =setq= を使用する（=org-capture-templates= 等） |
| 公開 API のみ使用 | サードパーティの =--= プレフィックスシンボルは =fboundp= ガード下でのみ使用 |
| named defun | 無名 lambda の =add-hook= 禁止。named defun を使うこと |
| docstring 必須 | 公開 defun は全てドキュメント文字列を持つ |
| =defgroup= 宣言 | =defcustom= を持つモジュールは =defgroup= を宣言 |

** ブート順序
:PROPERTIES:
:CUSTOM_ID: boot-sequence
:END:

#+begin_example
early-init.el
  → init.el (straight.el / leaf / org ブートストラップ)
      ① (defvar my:modules-extra nil)     ← personal ロードより先に宣言 (Fix B)
      ② personal/user.el をロード         (my:modules-extra に ui-visual-aids 等を追加)
      ③ (require 'modules nil t)           defcustom my:modules-extra は no-op
      ④ my/modules-load                    condition-case で隔離ロード
          core-* (16) → ui-* (11+α) → auth-* (1) → completion-* (12)
          → orgx-* (7) → vcs-* (3) → dev-* (13) → utils-* (9)
          → my:modules-extra の追加モジュール
      ⑤ after-init-hook                    core-switches: UI/LSP バンドル有効化
#+end_example

** ディレクトリ構造
:PROPERTIES:
:CUSTOM_ID: directory-structure
:END:

#+begin_example
.emacs.d/
├── early-init.el            GC・ディレクトリ定義・UI 早期設定
├── init.el                  straight.el・leaf・module 起動
├── modules.el               ロード順定義・エラー隔離
├── README.org               Literate source (唯一の真実)
├── Makefile                 ワンパス tangle → byte-compile ビルダー
├── lisp/core/               ランタイム基盤 (16 modules)
├── lisp/ui/                 視覚・操作 (11+α modules)
├── lisp/auth/               認証 (1 module)
├── lisp/completion/         補完 (12 modules)
├── lisp/orgx/               Org 拡張 (7 modules)
├── lisp/vcs/                バージョン管理 (3 modules)
├── lisp/dev/                開発ツール (13 modules)
├── lisp/utils/              汎用ヘルパー (9 modules)
├── personal/                ユーザー固有設定
├── .var/ (.etc/ .cache/)    no-littering: ランタイム状態 / 設定 / キャッシュ
├── .cache/eln/              native-comp ELN キャッシュ
└── .cache/treesit/          Tree-sitter 文法バイナリ
#+end_example

** 改訂履歴
:PROPERTIES:
:CUSTOM_ID: revision-history
:END:

| 版 | 日付 | 内容 |
|----|------|------|
| rev.1 | 2026-03 | 初版（全 81 モジュール記述） |
| rev.2 | 2026-03 | Fix A–D 反映: dev-lsp-bridge 移動・core-custom-ui-extras personal 化・completion-category-overrides 権威ソース明確化・bootstrap-- 命名 |
| rev.3 | 2026-03 | Fix A–AD 全反映（9ラウンド評価）。モジュール数 81→89 タングル対象・72 ロード順。setopt 統一・命名規則 my/xxx-- 廃止・docstring 50 件追加・defgroup 全モジュール整備・Coding Rules 文書化 |

*** rev.3 主要変更点

- *Fix E*: =ui-theme.el= の =ui-nano-palette= hard require → optional（=require nil t= + =with-eval-after-load=）
- *Fix G–I*: =treesit-*=・=native-comp-*= 等の =setopt= 統一
- *Fix J*: =aidermacs-extra-args= の =defvar= を使用前に移動
- *Fix K–N*: =setq= → =setopt= 統一（=save-place-file= 等）
- *Fix O*: =emms-directory= を =emms-all= より前に設定（=:pre-setq= 利用）
- *Fix P–S*: =dired/transient/corfu/org-roam= の =setopt= 統一
- *Fix T–V*: personal 層・toggle 関数の =setopt= 統一
- *Fix W*: 公開 =defun= 50 件にドキュメント文字列追加
- *Fix X/Y*: =my/xxx--= 形式 17 関数を =<module>--= 形式にリネーム（83 箇所）
- *Fix Z*: =utils-org-agenda.el= に =defgroup= 追加
- *Fix AA–AC*: =org-capture-templates= 等は backquote 展開・関数参照を含むため =setopt= の型チェッカーが誤検知。=setq= を使用（=setopt= への変更を撤回）。コーディングルールに例外条件を明記
- *Fix AD*: Coding Rules セクションに setopt・命名・docstring ルールを明文化

* 2. ブートストラップファイル
:PROPERTIES:
:CUSTOM_ID: bootstrap
:END:

** early-init.el
:PROPERTIES:
:CUSTOM_ID: early-init
:END:

| 項目 | 内容 |
|------|------|
| tangle 先 | =early-init.el= (root) |
| 実行タイミング | package.el 初期化より前 |

*** 責務

- =package.el= 無効化 (=package-enable-at-startup = nil=)
- GC・ファイルハンドラ起動時最適化 → =emacs-startup-hook= で復元
- ディレクトリ定義: =my:d=, =my:d:var=, =my:d:etc=, =my:d:cache=, =my:d:eln-cache=, =my:d:treesit=
- no-littering 互換変数 (=no-littering-var-directory= 等) の事前定義
- macOS Homebrew ツールチェーン環境変数設定
- =straight.el= ベースディレクトリ・プロファイル設定 (=setopt= 使用)
- ネイティブコンパイル ELN パス設定 (=setopt= 使用)
- 早期 UI デフォルト (tool-bar/menu-bar/scroll-bar 非表示)
- auto-save / backup を初期段階で無効化

*** 公開 API

- =my/ensure-directory-exists= Native Compilation 警告の収集・管理・診断 =core--ensure-directory= の公開 alias
- =core--ensure-directory= Native Compilation 警告の収集・管理・診断 実装本体
- =bootstrap--safe-load-file= Native Compilation 警告の収集・管理・診断 init.el 内部ヘルパー
- =my/safe-load-file= Native Compilation 警告の収集・管理・診断 =bootstrap--safe-load-file= の公開 alias

*** 備考

=core--ensure-directory= が実装。=my/ensure-directory-exists= は公開 alias。

** init.el
:PROPERTIES:
:CUSTOM_ID: init-el
:END:

| 項目 | 内容 |
|------|------|
| tangle 先 | =init.el= (root) |
| 実行タイミング | early-init.el 完了後 |

*** 責務

- =straight.el= ブートストラップ (=condition-case= でフォールバック)
- =leaf= / =leaf-keywords= インストール
- =org= 最新版インストール (straight.el 経由)
- =(defvar my:modules-extra nil)= Native Compilation 警告の収集・管理・診断 personal ロードより前に宣言
- =personal/user.el= ロード (=my:modules-extra= populate)
- =(require 'modules nil t)=
- =my/modules-load= 呼び出し

*** 公開 API

- =my/modules-load= Native Compilation 警告の収集・管理・診断 modules.el が提供

*** 備考

=bootstrap--safe-load-file= は init.el 内部ヘルパー。=my/safe-load-file= として公開 alias。

** modules.el
:PROPERTIES:
:CUSTOM_ID: modules-el
:END:

| 項目 | 内容 |
|------|------|
| tangle 先 | =modules.el= (root) |
| 実行タイミング | init.el ステップ ③ で require |

*** 責務

- =my:modules= (=defconst=) Native Compilation 警告の収集・管理・診断 72 モジュールの宣言的ロードリスト
- =my:modules-skip= (=defcustom=) Native Compilation 警告の収集・管理・診断 スキップリスト（削除安全性）
- =my:modules-extra= (=defcustom=) Native Compilation 警告の収集・管理・診断 personal オーバーレイ追加リスト
- =my/modules-load= Native Compilation 警告の収集・管理・診断 全モジュールを =condition-case= で隔離ロード
- ロード時間・成功/失敗ログを =after-init-hook= で出力

*** 公開 API

- =my/modules-load=
- =modules--should-load-p= （内部: Fix X/Y で =my/modules--should-load-p= → =modules--should-load-p= に改名）
- =modules--require-safe=
- =modules--format-seconds=

*** 備考

=my:modules-extra= defcustom の実体はここだが、init.el で =defvar= が先行宣言されるため no-op になる。

* 3. core/ Native Compilation 警告の収集・管理・診断 ランタイム基盤
:PROPERTIES:
:CUSTOM_ID: layer-core
:END:

| # | モジュール | 責務 | pub | cust | hook |
|---|-----------|------|-----|------|------|
| 1 | =core-fixes= | バージョン限定の最小互換性修正 | 0 | 0 | 0 |
| 2 | =core-policy= | lexical-binding 自動挿入ポリシー | 1 | 0 | 1 |
| 3 | =core-session= | バックグラウンドジョブスケジューラの公開ファサード | 5 | 3 | 0 |
| 4 | =core-session-private= | セッションジョブスケジューラの非公開実装 | 0 | 0 | 0 |
| 5 | =core-gc= | 長時間稼働セッション向け安全 GC フック | 0 | 1 | 2 |
| 6 | =core-persistence= | 古いバックアップファイルの自動削除 | 1 | 0 | 1 |
| 7 | =core-buffers= | *scratch* バッファの永続化（kill 後自動再生成） | 2 | 0 | 1 |
| 8 | =core-runtime= | OS 変数とパフォーマンス変数の起動時設定 | 0 | 1 | 1 |
| 9 | =core-tools= | 全レイヤー共有の低レベルヘルパー | 7 | 0 | 0 |
| 10 | =core-treesit= | Tree-sitter インフラ（利用可否・文法管理・モードリマップ） | 5 | 3 | 1 |
| 11 | =core-history= | セッション永続化（save-place/recentf/savehist） | 0 | 0 | 1 |
| 12 | =core-editing= | 構造的編集・undo・ウィンドウ・dirvish の設定 | 0 | 0 | 0 |
| 13 | =core-switches= | 起動時の UI バンドルと LSP バックエンドの選択 | 0 | 2 | 1 |
| 14 | =core-custom= | custom-file のパス管理と安全なロード | 2 | 0 | 0 |
| 15 | =core-api= | コアサービスへのクロスレイヤー安定 API | 3 | 0 | 0 |
| 16 | =core-native= | Native Compilation 警告の収集・管理・診断 | 0 | 0 | 0 |

** core/core-fixes.el
:PROPERTIES:
:CUSTOM_ID: core-fixes
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #1 |
| tangle 先 | =lisp/core/core-fixes.el= |
| provide | =core-fixes= |

*** 責務

ロード順 #1 として最初に読み込まれ、Emacs バージョンを限定した最小限の互換性修正を提供する。
=core-tools= はロード順 #9 のため、この時点では =core-tools-emacs>=​= はまだ定義されていない。
そのため =core-fixes--emacs>=​= を独立して定義する。=core-tools.el= の =core-tools-emacs>=​= と実装は同一だが、
=defalias= でロード順を逆転させるより意図的な重複を選択する（決定論的起動原則）。

*** 内部関数

- =core-fixes--emacs>==
  引数のバージョン番号と実行中の Emacs バージョンを比較し、条件を満たす場合に t を返す述語。=core-tools-emacs>=​= と実装は同一だが名前空間（内部 vs 公開）が異なる。

** core/core-policy.el
:PROPERTIES:
:CUSTOM_ID: core-policy
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #2 |
| tangle 先 | =lisp/core/core-policy.el= |
| defgroup | =core-policy= |
| provide | =core-policy= |

*** 責務

設定ファイル全体に =lexical-binding: t= を強制するポリシーを実装する。
=no-littering-var-directory= 配下の Emacs Lisp ファイルを =find-file-hook= で検知し、
ファイル先頭に =;;; -*- lexical-binding: t; -*-= ヘッダを自動挿入する。

*** 公開 API

- =my/auto-insert-lexical-binding=

*** フック登録

- =find-file-hook= → =my/auto-insert-lexical-binding)=

** core/core-session.el
:PROPERTIES:
:CUSTOM_ID: core-session
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #3 |
| tangle 先 | =lisp/core/core-session.el= |
| defgroup | =core-session= |
| provide | =core-session= |

*** 責務

バックグラウンドジョブスケジューラの公開ファサード層。
=core-session-start= / =core-session-stop= でライフサイクルを管理し、
=core-session-register-job= / =core-session-unregister-job= でアイドルレーンまたは定期レーンへのジョブ登録・解除を行う。
実装はすべて =core-session-private= に委譲する。外部モジュールは必ずこのファイルのみを =require= すること。

*** 公開 API

- =core-session-start=
- =core-session-stop=
- =core-session-register-job=
- =core-session-unregister-job=
- =core-session-run-job=

*** defcustom 変数

- =core-session-enable-p=
- =core-session-idle-delay=
- =core-session-periodic-interval=

*** 内部関数

- =core-session--ensure-started=
  セッションが未起動の場合にのみ =core-session-start= を呼び出す。二重起動を防ぐ安全ラッパー。

** core/core-session-private.el
:PROPERTIES:
:CUSTOM_ID: core-session-private
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #4 |
| tangle 先 | =lisp/core/core-session-private.el= |
| provide | =core-session-private= |

*** 責務

セッションジョブスケジューラの非公開実装。ジョブレジストリのハッシュテーブルを所有し、
アイドルタイマーと定期タイマーを管理する。ジョブメタデータの正規化と登録済みジョブの実行を担う。
=core-session.el= の内部詳細であり、外部モジュールから直接依存してはならない。

*** 内部関数

- =core-session--normalize-job-meta=
  ジョブ登録時に渡されたメタデータ（名前・レーン種別・遅延秒数）を正規化されたプロパティリストに変換する。
- =core-session--register=
  ジョブをレジストリハッシュテーブルに追加する。同名のジョブが存在する場合は上書きする。
- =core-session--unregister=
  ジョブをレジストリから削除する。対応するタイマーもキャンセルする。
- =core-session--jobs=
  登録済みジョブの全エントリをリストとして返す。
- =core-session--run-job=
  指定したジョブを =condition-case= で隔離して実行し、エラーを =message= に記録する。
- =core-session--run-all=
  登録済みの全ジョブを順次実行する。定期タイマーから呼び出される。
- =core-session--start=
  アイドルタイマーと定期タイマーを起動してスケジューラを開始する。
- =core-session--stop=
  両タイマーをキャンセルしてスケジューラを停止する。

** core/core-gc.el
:PROPERTIES:
:CUSTOM_ID: core-gc
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #5 |
| tangle 先 | =lisp/core/core-gc.el= |
| defgroup | =core-gc= |
| provide | =core-gc= |

*** 責務

長時間稼働セッション向けの安全な GC フックを提供する。
=focus-out-hook= と =minibuffer-exit-hook= で =garbage-collect= をトリガーし、
ユーザー操作の合間にメモリを解放する。=condition-case= でエラーを無音破棄し、
=core-gc-enable-p= をマスタースイッチとして機能させる。

*** フック登録

- =focus-out-hook= → =core-gc--collect)=
- =minibuffer-exit-hook= → =core-gc--collect)=

*** defcustom 変数

- =core-gc-enable-p=

*** 内部関数

- =core-gc--collect=
  =core-gc-enable-p= が non-nil の場合にのみ =garbage-collect= を呼び出す。=condition-case= でエラーを捕捉して無視する。

** core/core-persistence.el
:PROPERTIES:
:CUSTOM_ID: core-persistence
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #6 |
| tangle 先 | =lisp/core/core-persistence.el= |
| provide | =core-persistence= |

*** 責務

=my:d:var= ディレクトリ（フォールバックは =no-littering-var-directory=）のバックアップファイルを
起動時に自動クリーンアップする。7日以上経過したバックアップを削除し、ディレクトリの肥大化を防ぐ。
=emacs-startup-hook= で =my/delete-old-backups= を実行する。

*** 公開 API

- =my/delete-old-backups=

*** フック登録

- =emacs-startup-hook= → =my/delete-old-backups)=

** core/core-buffers.el
:PROPERTIES:
:CUSTOM_ID: core-buffers
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #7 |
| tangle 先 | =lisp/core/core-buffers.el= |
| provide | =core-buffers= |

*** 責務

=*scratch*= バッファが kill されたことを =kill-buffer-hook= で検知し、
=run-with-idle-timer 0= でアイドル時に =lisp-interaction-mode= で自動再生成する。
タイマーを介することで kill の完了を待ってから新バッファを作成し、競合を避ける。
フック関数を named defun にすることでデバッグ可能かつ安全に除去できる。

*** 公開 API

- =my/create-scratch-buffer=
- =my/kill-scratch-buffer-advice=

*** フック登録

- =kill-buffer-hook= → =core-buffers--maybe-recreate-scratch)=

*** 内部関数

- =core-buffers--maybe-recreate-scratch=
  =kill-buffer-hook= から呼ばれる。kill されたバッファが =*scratch*= の場合のみ =run-with-idle-timer 0= で =my/create-scratch-buffer= をスケジュールする。匿名ラムダでなく named defun であるため =remove-hook= で安全に除去できる。

** core/core-runtime.el
:PROPERTIES:
:CUSTOM_ID: core-runtime
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #8 |
| tangle 先 | =lisp/core/core-runtime.el= |
| defgroup | =core-runtime= |
| provide | =core-runtime= |

*** 責務

OS レベルのランタイム不変量と起動シーケンスを管理する。
macOS・Windows・その他の OS 判定でモディファイアキー変数を設定するが、キーバインドは定義しない
（UX は ui/ レイヤーの責務）。=after-init-hook= で GC 閾値とサブプロセス読み込みチャンクサイズを設定する。
このモジュールはユーザー可視の副作用を持ってはならない。

*** フック登録

- =after-init-hook= → =core-runtime--startup-init)=

*** defcustom 変数

- =core-runtime-configure-modifiers-p=

*** 内部関数

- =core-runtime--configure-modifiers=
  OS を判定してモディファイアキー変数（=mac-command-modifier=・=mac-option-modifier= 等）を設定する。キーバインドの登録は行わない。
- =core-runtime--startup-init=
  =after-init-hook= から呼ばれる。GC 閾値（=gc-cons-threshold=）・サブプロセス読み込みバイト数（=read-process-output-max=）などのパフォーマンス変数を最終値に設定する。

** core/core-tools.el
:PROPERTIES:
:CUSTOM_ID: core-tools
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #9 |
| tangle 先 | =lisp/core/core-tools.el= |
| provide | =core-tools= |

*** 責務

全レイヤーで共有する低レベルヘルパーユーティリティを提供する。
バージョン述語・ディレクトリ作成エイリアス・フィーチャ/関数の存在確認述語・
安全な =require= ラッパー・=provide= 宣言の確認・ユーザー設定ファイル判定を含む。
=core-fixes--emacs>=​= を公開 =core-tools-emacs>=​= として defalias することで重複実装を解消する。

*** 公開 API

- =core-tools-emacs>==
- =core-tools-feature-present-p=
- =core-tools-function-present-p=
- =core-tools-require-if-present=
- =core-tools-call-if-present=
- =core-tools-check-provide=
- =core-tools-user-file-p=

** core/core-treesit.el
:PROPERTIES:
:CUSTOM_ID: core-treesit
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #10 |
| tangle 先 | =lisp/core/core-treesit.el= |
| defgroup | =core-treesit= |
| provide | =core-treesit= |

*** 責務

Tree-sitter のインフラ全体を管理する。=core-treesit-enable-p= で全機能をガードし、
文法ソース URL を =core-treesit-register-sources= で登録する。
=core-treesit-apply-remap= で利用可能な文法に対応するレガシーメジャーモードを =-ts-mode= バリアントへ自動リマップする。
=core-treesit-install-grammar= と =core-treesit-install-all= でオンデマンドまたは一括文法インストールを提供し、
=core-treesit-auto-install-p= が non-nil の場合は起動時に不足文法を自動インストールする。

*** 公開 API

- =core-treesit-apply-remap=
- =core-treesit-register-sources=
- =core-treesit-install-grammar=
- =core-treesit-install-all=
- =core-treesit-report=

*** フック登録

- =emacs-startup-hook= → =core-treesit--startup-init)=

*** defcustom 変数

- =core-treesit-enable-p=
- =core-treesit-auto-install-p=
- =core-treesit-install-languages=

*** 内部関数

- =core-treesit--available-p=
  Emacs が Tree-sitter をサポートしているか（=treesit-available-p=）と =core-treesit-enable-p= の両方を確認し、Tree-sitter を使用可能な状態かを返す。
- =core-treesit--language-available-p=
  指定した言語の文法バイナリがすでにインストール済みかを =treesit-language-available-p= で確認する。
- =core-treesit--startup-init=
  =emacs-startup-hook= から呼ばれる。モードリマップの適用と =core-treesit-auto-install-p= が有効な場合の文法自動インストールを実行する。

** core/core-history.el
:PROPERTIES:
:CUSTOM_ID: core-history
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #11 |
| tangle 先 | =lisp/core/core-history.el= |
| provide | =core-history= |

*** 責務

セッションの永続化機能をまとめて設定する。=saveplace=・=recentf=・=savehist= のパスを
=no-littering-var-directory= 配下に設定し、=my:desktop-ask-on-restore= を savehist で
セッション間に永続化する。=emacs-startup-hook= で3つのモードをまとめて有効化する。

*** フック登録

- =emacs-startup-hook= → =core-history--startup-init)=

*** 内部関数

- =core-history--startup-init=
  =emacs-startup-hook= から呼ばれる。=save-place-mode=・=recentf-mode=・=savehist-mode= を順次有効化し、=auto-revert-mode= のグローバル設定を適用する。

** core/core-editing.el
:PROPERTIES:
:CUSTOM_ID: core-editing
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #12 |
| tangle 先 | =lisp/core/core-editing.el= |
| provide | =core-editing= |

*** 責務

編集体験の基盤となるパッケージ群を設定する。TRAMP を =scp= デフォルトで設定し、
=auto-save-visited-mode= で継続的自動保存を有効化する。=paredit= / =puni= による構造的編集、
=show-paren=・=undo-fu= / =vundo=・=ace-window=・=visual-line-mode= を設定し、
=dirvish-override-dired-mode= で dired を dirvish に置き換える。
=expand-region= でセマンティックなテキスト選択を提供する。

*** 内部関数

- =core-editing--paredit-setup=
  paredit の有効化時に =electric-pair-mode= を無効化して競合を防ぐ。バッファローカルで適用する。
- =core-editing--puni-minibuffer-off=
  ミニバッファでは puni の構造的削除が誤動作するため、=puni-mode= をバッファローカルで無効化する。

** core/core-switches.el
:PROPERTIES:
:CUSTOM_ID: core-switches
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #13 |
| tangle 先 | =lisp/core/core-switches.el= |
| defgroup | =my:switches= |
| provide | =core-switches= |

*** 責務

起動時の UI バンドル（=none= / =doom= / =nano=）と LSP バックエンド（=eglot= / =lsp=）を
=my:use-ui= / =my:use-lsp= で選択し、=after-init-hook= で有効化する。
4つの有効化関数はオートロード宣言により遅延ロードされる。
personal オーバーレイはこのモジュールより前に =my:use-ui= と =my:use-lsp= を設定すること。

*** フック登録

- =after-init-hook= → =core-switches--activate)=

*** defcustom 変数

- =my:use-lsp=
- =my:use-ui=

*** 内部関数

- =core-switches--present-p=
  指定したシンボル（UI または LSP バックエンドの有効化関数）が実際にロード可能かを =fboundp= または =featurep= で確認する。
- =core-switches--enable-ui=
  =my:use-ui= の値（=doom= / =nano= / =none=）に応じた UI バンドルの有効化関数を呼び出す。
- =core-switches--enable-lsp=
  =my:use-lsp= の値（=eglot= / =lsp=）に応じた LSP バックエンドの有効化関数を呼び出す。
- =core-switches--activate=
  =after-init-hook= から呼ばれる。=core-switches--enable-ui= と =core-switches--enable-lsp= を順次呼び出してバンドルを起動する。

** core/core-custom.el
:PROPERTIES:
:CUSTOM_ID: core-custom
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #14 |
| tangle 先 | =lisp/core/core-custom.el= |
| provide | =core-custom= |

*** 責務

=custom-file= を =my:d:etc/custom.el= に解決し、ディレクトリとファイルが存在しなければ作成する。
=ignore-errors= でエラーを抑制しながら =custom.el= をロードする。
=my/custom-open= でファイルをインタラクティブに開き、
=my/custom-dump-current= で未保存のカスタマイズ内容を確認できる。

*** 公開 API

- =my/custom-open= =;;;###autoload=
- =my/custom-dump-current= =;;;###autoload=

*** 内部関数

- =core-custom--ensure-file=
  =my:d:etc= ディレクトリと =custom.el= ファイルの存在を確認し、どちらかが存在しなければ =make-directory= / =write-region= で作成する。

** core/core-api.el
:PROPERTIES:
:CUSTOM_ID: core-api
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #15 |
| tangle 先 | =lisp/core/core-api.el= |
| provide | =core-api= |

*** 責務

コアサービスへのクロスレイヤー安定公開 API 層。セッションジョブ操作を
=core-api-session-*= ラッパーとして再エクスポートする。
すべての呼び出しを =fboundp= でガードし、=core-session= が無効または欠損していても安全にロードできる。
他レイヤーは =core-session= に直接依存せず、必ずこの API を経由すること。

*** 公開 API

- =core-api-session-register-job=
- =core-api-session-unregister-job=
- =core-api-session-run-job=

** core/core-native.el
:PROPERTIES:
:CUSTOM_ID: core-native
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #16 |
| tangle 先 | =lisp/core/core-native.el= |

* 4. ui/ Native Compilation 警告の収集・管理・診断 ビジュアル・操作
:PROPERTIES:
:CUSTOM_ID: layer-ui
:END:

| # | モジュール | 責務 | pub | cust | hook |
|---|-----------|------|-----|------|------|
| 17 | =ui-font= | モノスペース・可変ピッチ・絵文字・リガチャのフォント設定 | 3 | 4 | 2 |
| 18 | =ui-theme= | テーマ・フェイス・スペーシングの設定 | 0 | 2 | 3 |
| 19 | =ui-window= | zoom・desktop・winner・レイアウトスナップショット | 2 | 0 | 0 |
| 20 | =ui-utils= | モードライン・時計・Treemacs・クリップボード統合 | 0 | 0 | 0 |
| 21 | =ui-health-modeline= | セッション健全性指標のモードライン表示 | 0 | 3 | 4 |
| 22 | =ui-imenu= | Imenu によるシンボルナビゲーションのサイドパネル統合 | 0 | 0 | 0 |
| 23 | =ui-leader= | 階層的リーダーキーインフラの定義 | 1 | 3 | 0 |
| 24 | =ui-hydra= | UI 層で使用するオプショナル Hydra ヘルパー | 2 | 1 | 0 |
| 25 | =ui-which-key= | リーダーキーの which-key 統合 | 0 | 2 | 1 |
| 26 | =ui-keymap= | リーダーキーバインドとキーボード変換ヘルパー | 0 | 1 | 1 |
| 27 | =ui-global-keys= | リーダーキー体系外のグローバルキーバインド登録 | 0 | 0 | 1 |

** ui/ui-font.el
:PROPERTIES:
:CUSTOM_ID: ui-font
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #17 |
| tangle 先 | =lisp/ui/ui-font.el= |
| defgroup | =ui-font= |
| provide | =ui-font= |

*** 責務

モノスペース・可変ピッチ・絵文字・リガチャの各フォントを設定する。
=ui-font--system-default= と =ui-font--system-emoji= で OS 判定のフォールバックを提供し、
=ui-font-apply= をフォント適用エントリポイントとして =after-init-hook= と
=server-after-make-frame-hook= の両方で呼び出すことでデーモン起動時のフレーム遅延に対応する。
グラフィカルフレームでは =global-ligature-mode= を有効化する。

*** 公開 API

- =ui-font-apply=
- =ui-font-show-current= =;;;###autoload=
- =ui-font-describe= =;;;###autoload=

*** フック登録

- =after-make-frame-functions= → =ui-font--apply-to-frame)=
- =after-init-hook= → =ui-font-apply))=

*** defcustom 変数

- =ui-font-default=
- =ui-font-variable-pitch=
- =ui-font-emoji=
- =ui-font-size=

*** 内部関数

- =ui-font--system-default=
  OS 判定（macOS / Linux / Windows）に基づきデフォルトのモノスペースフォント名を返す。=ui-font-default= のフォールバック値として使用される。
- =ui-font--system-emoji=
  OS に応じた絵文字フォント名を返す（macOS: "Apple Color Emoji"、Linux: "Noto Color Emoji" 等）。
- =ui-font--apply-to-frame=
  =after-make-frame-functions= から呼ばれる。引数のフレームがグラフィカルフレームの場合のみ =ui-font-apply= を呼び出す。

** ui/ui-theme.el
:PROPERTIES:
:CUSTOM_ID: ui-theme
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #18 |
| tangle 先 | =lisp/ui/ui-theme.el= |
| defgroup | =ui-theme= |
| provide | =ui-theme= |

*** 責務

テーマ設定とフェイス適用を担う UI 層のモジュール。Nano テーマをロードし、
=ui-nano-palette= 経由でパレットオーバーライドを適用する。ペアリングハイライトフェイスと
=spacious-padding= を設定する。設計目標: 決定論的初期化・デーモン安全なフレーム処理・
テーマリロード安全性・Emacs 30/31 前方互換性。=ui-nano-palette= への依存はオプショナルであり、
=my:use-ui='doom= または ='none= でも安全にロードされる。

*** フック登録

- =after-make-frame-functions= → =ui-theme--apply-frame)=
- =enable-theme-functions= → =ui-theme--after-theme)=
- =emacs-startup-hook= → =ui-theme--init)=

*** defcustom 変数

- =ui-theme-enable-p=
- =ui-theme-line-spacing=

*** 内部関数

- =ui-theme--safe-face=
  指定したフェイスが存在する場合のみ =set-face-attribute= を呼び出す。存在しないフェイスへのアクセスによるエラーを防ぐ安全ラッパー。
- =ui-theme--apply-faces=
  パレットの値を読み取りフェイスに適用する。=my/nano-apply-faces= が =fboundp= の場合のみ呼び出すことで nano 非使用環境でも安全。
- =ui-theme--apply-frame=
  =after-make-frame-functions= から呼ばれる。新規フレームに対してフェイスを再適用する。デーモン起動時の遅延フレーム生成に対応する。
- =ui-theme--after-theme=
  =enable-theme-functions= から呼ばれる。テーマ切り替え後にフェイスの上書き設定を再適用してテーマの意図しない変更を防ぐ。
- =ui-theme--init=
  =emacs-startup-hook= から呼ばれる。テーマのロードとフェイス初期適用を実行する。

** ui/ui-window.el
:PROPERTIES:
:CUSTOM_ID: ui-window
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #19 |
| tangle 先 | =lisp/ui/ui-window.el= |
| provide | =ui-window= |

*** 責務

zoom・desktop・winner・レイアウトスナップショット. zoom によるゴールデンレシオ型自動ウィンドウサイズ調整、=desktop-save-mode= によるセッション間レイアウト永続化、=winner-mode= によるウィンドウ構成の undo/redo を設定する。=my:saved-window-config= に名前付きレイアウトスナップショットを保存・復元する =my/save-window-layout= / =my/restore-window-layout= を提供する。

*** 公開 API

- =my/save-window-layout=
- =my/restore-window-layout=

** ui/ui-utils.el
:PROPERTIES:
:CUSTOM_ID: ui-utils
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #20 |
| tangle 先 | =lisp/ui/ui-utils.el= |
| provide | =ui-utils= |

*** 責務

モードライン・時計・Treemacs・クリップボード統合 Provides: - mode line compaction via minions - time and battery display - Treemacs configuration - Dired icons - macOS clipboard integration コア UI ポリシーの定義は意図的に避け、補助的な統合のみを担う。

** ui/ui-health-modeline.el
:PROPERTIES:
:CUSTOM_ID: ui-health-modeline
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #21 |
| tangle 先 | =lisp/ui/ui-health-modeline.el= |
| defgroup | =ui-health-modeline= |
| provide | =ui-health-modeline= |

*** 責務

セッションの健全性指標をモードラインに軽量表示する。バッファ数・実行中プロセス数・
Eglot 管理バッファ数をモードライン文字列として構築し、重いインストルメンテーションなしに
セッション負荷とバックグラウンドアクティビティを可視化する。

*** フック登録

- =buffer-list-update-hook= → =ui-health--update)=
- =process-status-change-hook= → =ui-health--update)=
- =eglot-managed-mode-hook= → =ui-health--update)=
- =eglot-shutdown-hook= → =ui-health--update))=

*** defcustom 変数

- =ui-health-show-buffers-p=
- =ui-health-show-processes-p=
- =ui-health-show-eglot-p=

*** 内部関数

- =ui-health--buffers-count=
  =buffer-list= の長さを返す。内部バッファ（=*Messages*= 等）を含む全バッファ数。
- =ui-health--processes-count=
  =process-list= の長さを返す。実行中のすべてのサブプロセス（LSP サーバー含む）の数。
- =ui-health--eglot-count=
  eglot が管理しているバッファ数を返す。=eglot-managed-buffers= が利用可能な場合はそれを使用する。
- =ui-health--update=
  各カウンターを再計算してモードライン文字列を更新し、=force-mode-line-update= を呼び出す。バッファリスト・プロセス状態・eglot の各フックから呼び出される。

** ui/ui-imenu.el
:PROPERTIES:
:CUSTOM_ID: ui-imenu
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #22 |
| tangle 先 | =lisp/ui/ui-imenu.el= |
| provide | =ui-imenu= |

*** 責務

Imenu によるシンボルナビゲーションのサイドパネル統合 Provides a side-panel symbol outline using `imenu-list'. Imenu のセマンティクスは変更せず、大きなバッファのナビゲーション支援に特化する。左側シンボルツリー・有効化時の自動フォーカス・Imenu 対応メジャーモードとの軽量統合を提供する。設定は最小限に保つ。

** ui/ui-leader.el
:PROPERTIES:
:CUSTOM_ID: ui-leader
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #23 |
| tangle 先 | =lisp/ui/ui-leader.el= |
| defgroup | =ui-leader= |
| provide | =ui-leader= |

*** 責務

階層的リーダーキーインフラを定義する。キーマップと =C-c SPC= ルートプレフィックスのグローバルインストールのみを行い、
実際のコマンドバインドは行わない。レイアウトは安定かつ削除安全に設計されており、
他モジュールがこれらのマップにコマンドをバインドする。

*** 公開 API

- =ui-leader-local-key=

*** defcustom 変数

- =ui-leader-key=
- =ui-leader-local-suffix=
- =ui-leader-enable-p=

*** 内部関数

- =ui-leader--ensure-prefix-map=
  指定したキー（例: ="b"=）に対応するサブキーマップが存在しない場合にのみ新規作成する。べき等性を保つ。
- =ui-leader--define-maps=
  b/w/p/s/g/c/e/t/o/m/a/q/h の各サブキーマップを =ui-leader--ensure-prefix-map= で一括作成する。
- =ui-leader--bind-root=
  =C-c SPC= にルートリーダーキーマップをバインドする。=after-init-hook= 後ではなくモジュールロード時に実行する。

** ui/ui-hydra.el
:PROPERTIES:
:CUSTOM_ID: ui-hydra
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #24 |
| tangle 先 | =lisp/ui/ui-hydra.el= |
| defgroup | =ui-hydra= |
| provide | =ui-hydra= |

*** 責務

UI 層で使用するオプショナル Hydra ヘルパー Hydras provide small transient keymaps for interactive adjustments such as text scaling. Hydra パッケージが未インストールの場合も正常動作するよう、Hydra を初回呼び出し時のみ遅延定義する。

*** 公開 API

- =ui-hydra-define-text-scale=
- =ui-hydra-text-scale/body=

*** defcustom 変数

- =ui-hydra-enable-p=

** ui/ui-which-key.el
:PROPERTIES:
:CUSTOM_ID: ui-which-key
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #25 |
| tangle 先 | =lisp/ui/ui-which-key.el= |
| defgroup | =ui-which-key= |
| provide | =ui-which-key= |

*** 責務

リーダーキーインフラの which-key 統合を設定する。Emacs 30+ では組み込みパッケージ、
それ以前ではスタンドアロンパッケージを使用する。which-key の有効化・リーダーキープレフィックスラベル・
アイドル遅延を設定する。リーダーバインドの定義は行わず、発見可能性の向上のみを担う。

*** フック登録

- =after-init-hook= → =ui-which-key--startup-init)=

*** defcustom 変数

- =ui-which-key-enable-p=
- =ui-which-key-idle-delay=

*** 内部関数

- =ui-which-key--label-leader-groups=
  which-key の置換ルールを設定し、リーダーキーのサブマップに人間可読なラベル（例: "b → buffers"）を付与する。
- =ui-which-key--startup-init=
  =after-init-hook= から呼ばれる。which-key モードの有効化とラベル設定を行う。

** ui/ui-keymap.el
:PROPERTIES:
:CUSTOM_ID: ui-keymap
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #26 |
| tangle 先 | =lisp/ui/ui-keymap.el= |
| defgroup | =ui-keyboard= |
| provide | =ui-keymap= |

*** 責務

=ui-leader.el= が定義したキーマップへのコマンドバインドと、キーボード変換ヘルパーを設定する。
グローバルリーダーバインド・ローカルリーダーバインド・オプションの =C-h= → =DEL= 変換を行う。
リーダーキーインフラ自体の定義は =ui-leader.el= の責務。

*** フック登録

- =after-init-hook= → =ui-keymap--startup-init)=

*** defcustom 変数

- =ui-keyboard-map-c-h-to-del=

*** 内部関数

- =ui-keyboard--apply=
  キーボード変換テーブルを実際に適用する。=ui-keyboard-map-c-h-to-del= が non-nil の場合 =C-h= を =DEL= に変換する。
- =ui-keyboard--watcher=
  フレーム作成時など変換テーブルがリセットされる可能性のある場面で =ui-keyboard--apply= を再実行するウォッチャー。
- =ui-keymap--bind=
  1つのキーバインドを指定キーマップに登録するユーティリティ関数。
- =ui-keymap--leader-bind=
- =ui-keymap--leader-binds=
  グローバルリーダーキーマップへのコマンドバインドを一括登録する。
- =ui-keymap--dirvish-open-read-only=
  dirvish でマークされたファイルを読み取り専用で開く補助コマンド。
- =ui-keymap--local-leader-binds=
  メジャーモード向けのローカルリーダーバインドを登録する。
- =ui-keymap--startup-init=
  =after-init-hook= から呼ばれる。すべてのバインド登録と変換テーブル適用を実行する。

** ui/ui-global-keys.el
:PROPERTIES:
:CUSTOM_ID: ui-global-keys
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #27 |
| tangle 先 | =lisp/ui/ui-global-keys.el= |
| provide | =ui-global-keys= |

*** 責務

リーダーキー体系外のグローバルキーバインドを =after-init-hook= で登録する。
=C-s=・=C-c b/k/o/r=・=C-.=・=C-?= の基本ナビゲーション/編集、
macOS スタイルのバッファナビゲーション（=s-left/right/up/down=）、=<f1>= を =help-command= に割り当てる。

*** フック登録

- =after-init-hook= → =ui-global-keys--startup-init)=

*** 内部関数

- =ui-global-keys--startup-init=
  =after-init-hook= から呼ばれる。=global-set-key= を用いて全グローバルキーバインドを一括登録する。

* 5. auth/ Native Compilation 警告の収集・管理・診断 認証基盤
:PROPERTIES:
:CUSTOM_ID: layer-auth
:END:

| # | モジュール | 責務 | pub | cust | hook |
|---|-----------|------|-----|------|------|
| 28 | =auth-core= | GPG/pass ベースの認証環境設定 | 1 | 1 | 1 |

** auth/auth-core.el
:PROPERTIES:
:CUSTOM_ID: auth-core
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #28 |
| tangle 先 | =lisp/auth/auth-core.el= |
| defgroup | =auth-core= |
| provide | =auth-core= |

*** 責務

設定全体の認証インフラを提供する。必要な認証環境変数を検証し、
EasyPG ファイル暗号化サポートを有効化し、pass ベースの auth-source バックエンドを有効化する。
必要な外部ツール（=gpg=・=pass=）が利用可能な場合のみ設定が有効になる。

*** 公開 API

- =auth-core-check-env=

*** フック登録

- =after-init-hook= → =auth-core--startup-init)=

*** defcustom 変数

- =auth-core-enable-p=

*** 内部関数

- =auth-core--startup-init=
  =after-init-hook= から呼ばれる。=gpg= と =pass= の存在を確認し、利用可能であれば =epa-file-enable= と =auth-source-pass-enable= を呼び出す。

* 6. completion/ Native Compilation 警告の収集・管理・診断 補完スタック
:PROPERTIES:
:CUSTOM_ID: layer-completion
:END:

| # | モジュール | 責務 | pub | cust | hook |
|---|-----------|------|-----|------|------|
| 29 | =completion-core= | orderless 補完スタイルの基盤設定 | 0 | 0 | 0 |
| 30 | =completion-vertico= | posframe・marginalia 付き Vertico ミニバッファ UI | 0 | 0 | 1 |
| 31 | =completion-consult= | consult による強化検索とクロスリファレンスナビゲーション | 0 | 0 | 0 |
| 32 | =completion-embark= | Consult 統合付き Embark コンテキストアクション | 0 | 0 | 0 |
| 33 | =completion-corfu= | kind-icon・CAPE 付きバッファ内ポップアップ補完 | 0 | 0 | 1 |
| 34 | =completion-icons= | ibuffer・補完 UI 向け nerd-icons 統合 | 0 | 0 | 0 |
| 35 | =completion-capf= | モード別 CAPF スタックと category-overrides の権威定義 | 0 | 0 | 9 |
| 36 | =completion-capf-org-src= | Org SRC 編集バッファの CAPF ディスパッチ | 0 | 0 | 1 |
| 37 | =completion-capf-org-src-lang= | Org SRC ブロック内の言語別 CAPF スタック | 0 | 0 | 1 |
| 38 | =completion-corfu-org-src= | Org SRC 編集バッファ内の Corfu チューニング | 0 | 0 | 1 |
| 39 | =completion-orderless-org-src= | Org SRC 編集バッファ内の orderless チューニング | 0 | 0 | 1 |
| 40 | =completion-lsp= | プロジェクト切替時の obsolete Eglot サーバークリーンアップ | 1 | 1 | 2 |

** completion/completion-core.el
:PROPERTIES:
:CUSTOM_ID: completion-core
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #29 |
| tangle 先 | =lisp/completion/completion-core.el= |
| provide | =completion-core= |

*** 責務

補完スタイルの基盤を設定する。=orderless= をインストールし、
=completion-styles= を =orderless= + =basic= に設定する。
=completion-category-overrides= はここでは設定しない。正典テーブルは =completion-capf.el= に集約されており、
両ファイルに定義すると最後書き込みセマンティクスで =completion-capf.el= が黙って上書きし、
このファイルの宣言が誤解を招くためである。

** completion/completion-vertico.el
:PROPERTIES:
:CUSTOM_ID: completion-vertico
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #30 |
| tangle 先 | =lisp/completion/completion-vertico.el= |
| provide | =completion-vertico= |

*** 責務

vertico・marginalia・vertico-posframe を =window-setup-hook= で正確に1回だけ初期化し、
二重初期化を防ぐ。posframe 表示はグラフィカルセッションのみで有効化する。
vertico 候補数と posframe 境界幅を設定する。

*** フック登録

- =window-setup-hook= → =completion-vertico--init)=

*** 内部関数

- =completion-vertico--init=
  =window-setup-hook= から呼ばれる。=vertico-mode=・=marginalia-mode= を有効化し、グラフィカルフレームの場合のみ =vertico-posframe-mode= を有効化する。実行後フックから自身を除去して二重実行を防ぐ。

** completion/completion-consult.el
:PROPERTIES:
:CUSTOM_ID: completion-consult
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #31 |
| tangle 先 | =lisp/completion/completion-consult.el= |
| provide | =completion-consult= |

*** 責務

consult による強化された検索とクロスリファレンスナビゲーションを設定する。
=xref-show-xrefs-function= と =xref-show-definitions-function= を =consult-xref= 経由にルーティングし、
より豊富な候補表示を実現する。

** completion/completion-embark.el
:PROPERTIES:
:CUSTOM_ID: completion-embark
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #32 |
| tangle 先 | =lisp/completion/completion-embark.el= |
| provide | =completion-embark= |

*** 責務

Consult と統合した Embark コンテキストアクションを設定する。
=prefix-help-command= を =embark-prefix-help-command= に設定し、
=embark-collect= バッファで =consult-preview-at-point-mode= を有効化する。

** completion/completion-corfu.el
:PROPERTIES:
:CUSTOM_ID: completion-corfu
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #33 |
| tangle 先 | =lisp/completion/completion-corfu.el= |
| provide | =completion-corfu= |

*** 責務

corfu・kind-icon・cape を =window-setup-hook= で正確に1回だけ初期化する。
=global-corfu-mode= を有効化し =tab-always-indent 'complete= を設定する。
=cape-elisp-symbol=・=cape-file=・=cape-dabbrev= を CAPF として登録し、
kind-icon をマージンフォーマッタとして設定する。

*** フック登録

- =window-setup-hook= → =completion-corfu--init)=

*** 内部関数

- =completion-corfu--init=
  =window-setup-hook= から呼ばれる。corfu・kind-icon・cape を初期化する。=remove-hook= で自身をフックから除去して二重実行を防ぐ。対象は =window-setup-hook= であり =completion-at-point-functions= ではない。

** completion/completion-icons.el
:PROPERTIES:
:CUSTOM_ID: completion-icons
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #34 |
| tangle 先 | =lisp/completion/completion-icons.el= |
| provide | =completion-icons= |

*** 責務

ibuffer・補完 UI 向け nerd-icons 統合 ibuffer バッファで =nerd-icons-ibuffer-mode= を有効化し、=marginalia-mode= フックに =nerd-icons-completion-marginalia-setup= を登録する。 =marginalia-align= は =completion-vertico.el= の leaf marginalia ブロックで設定する（ダウンストリームファイルへの bare setq より marginalia をインストールする leaf ブロックに設定を持つほうが明確）。

** completion/completion-capf.el
:PROPERTIES:
:CUSTOM_ID: completion-capf
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #35 |
| tangle 先 | =lisp/completion/completion-capf.el= |
| provide | =completion-capf= |

*** 責務

モード別の =completion-at-point-functions= （CAPF）スタックを定義し、対応するモードフックに登録する。
対象: emacs-lisp・prog・text・org・shell・repl。
=completion-category-overrides= の正典テーブルをこのファイルが唯一の権威として定義する。
=completion-core.el= の基本 orderless スタイルを前提にファイル・シンボル・keyword・tex・dabbrev の
カテゴリ別ルールを拡張する。単一権威ソースにより最後書き込み衝突を防ぐ。

*** フック登録

- =emacs-lisp-mode-hook= → =completion--elisp-capfs)=
- =lisp-interaction-mode-hook= → =completion--elisp-capfs)=
- =prog-mode-hook= → =completion--prog-capfs)=
- =text-mode-hook= → =completion--text-capfs)=
- =org-mode-hook= → =completion--org-capfs)=
- =sh-mode-hook= → =completion--shell-capfs)=
- =eshell-mode-hook= → =completion--shell-capfs)=
- =ielm-mode-hook= → =completion--repl-capfs)=
- =comint-mode-hook= → =completion--repl-capfs)=

*** 内部関数

- =completion--set-capfs=
  バッファローカルな =completion-at-point-functions= を指定のリストに設定するユーティリティ。各モード固有の CAPF スタッカーから呼ばれる。
- =completion--elisp-capfs=
  Emacs Lisp モード向け CAPF スタック（=cape-elisp-symbol=・=elisp-completion-at-point= 等）を設定する。
- =completion--prog-capfs=
  汎用プログラミングモード向け CAPF スタックを設定する。LSP 系 CAPF を先頭に配置する。
- =completion--text-capfs=
  テキストモード向け CAPF スタック（=cape-dabbrev=・=cape-file= 等）を設定する。
- =completion--org-capfs=
  Org モード向け CAPF スタックを設定する。org 固有の補完と汎用 CAPF を組み合わせる。
- =completion--shell-capfs=
  シェルモード・eshell 向け CAPF スタックを設定する。
- =completion--repl-capfs=
  IELM・comint 等の REPL モード向け CAPF スタックを設定する。

** completion/completion-capf-org-src.el
:PROPERTIES:
:CUSTOM_ID: completion-capf-org-src
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #36 |
| tangle 先 | =lisp/completion/completion-capf-org-src.el= |
| provide | =completion-capf-org-src= |

*** 責務

Org SRC 編集バッファの CAPF ディスパッチャー。=org-src-mode-hook= で派生メジャーモードを判定し、
elisp 系なら =completion--elisp-capfs=、prog 系なら =completion--prog-capfs=、
それ以外は =completion--text-capfs= を選択する。

*** フック登録

- =org-src-mode-hook= → =completion--org-src-capfs)=

*** 内部関数

- =completion--org-src-capfs=
  =org-src-mode-hook= から呼ばれる。=major-mode= を調べてモードに適した CAPF スタックを選択・適用する。

** completion/completion-capf-org-src-lang.el
:PROPERTIES:
:CUSTOM_ID: completion-capf-org-src-lang
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #37 |
| tangle 先 | =lisp/completion/completion-capf-org-src-lang.el= |
| provide | =completion-capf-org-src-lang= |

*** 責務

Org SRC ブロック内の言語別 CAPF スタックを提供する。
shell・SQL・Python の各モードに細粒度の CAPF スタックを定義し、
=org-src-mode-hook= でディスパッチする。

*** フック登録

- =org-src-mode-hook= → =completion--org-src-lang-capfs)=

*** 内部関数

- =completion--org-src-shell-capfs=
  Org SRC 内の shell ブロック向け CAPF スタックを設定する。
- =completion--org-src-sql-capfs=
  Org SRC 内の SQL ブロック向け CAPF スタックを設定する。
- =completion--org-src-python-capfs=
  Org SRC 内の Python ブロック向け CAPF スタックを設定する。
- =completion--org-src-lang-capfs=
  =org-src-mode-hook= から呼ばれる。言語名を調べて各言語固有の CAPF スタッカーを呼び出すディスパッチャー。

** completion/completion-corfu-org-src.el
:PROPERTIES:
:CUSTOM_ID: completion-corfu-org-src
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #38 |
| tangle 先 | =lisp/completion/completion-corfu-org-src.el= |
| provide | =completion-corfu-org-src= |

*** 責務

Org SRC 編集バッファ内の Corfu をチューニングする。
=corfu-auto-delay= を 0.1 秒・=corfu-auto-prefix= を 1 に縮小してコードブロック内の
補完応答性を高め、=corfu-cycle= を無効化する。

*** フック登録

- =org-src-mode-hook= → =completion--org-src-corfu-setup)=

*** 内部関数

- =completion--org-src-corfu-setup=
  =org-src-mode-hook= から呼ばれる。=corfu-auto-delay=・=corfu-auto-prefix=・=corfu-cycle= をバッファローカルで上書きする。

** completion/completion-orderless-org-src.el
:PROPERTIES:
:CUSTOM_ID: completion-orderless-org-src
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #39 |
| tangle 先 | =lisp/completion/completion-orderless-org-src.el= |
| provide | =completion-orderless-org-src= |

*** 責務

Org SRC 編集バッファ内の orderless をチューニングする。
バッファローカルな =completion-category-overrides= を設定し、シンボル・keyword・関数・変数カテゴリは
orderless を使用し、ファイルと dabbrev は basic/partial-completion を使用する。

*** フック登録

- =org-src-mode-hook= → =completion--org-src-orderless-setup)=

*** 内部関数

- =completion--org-src-orderless-setup=
  =org-src-mode-hook= から呼ばれる。=completion-category-overrides= をバッファローカルで上書きし SRC ブロック内の補完スタイルを最適化する。

** completion/completion-lsp.el
:PROPERTIES:
:CUSTOM_ID: completion-lsp
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #40 |
| tangle 先 | =lisp/completion/completion-lsp.el= |
| defgroup | =completion-lsp= |
| provide | =completion-lsp= |

*** 責務

プロジェクト切り替え時に obsolete な Eglot サーバーを防衛的にクリーンアップする。
=eglot-current-server= / =eglot-managed-buffers= などの公開 API のみを使用し、
内部変数（=eglot--servers=）への依存を排除している（Fix 1）。
=eglot--project= も内部関数のため =fboundp= でガードし Emacs バージョン間の安全性を確保する（Fix 5）。

*** 公開 API

- =completion-lsp-on-project-switch=

*** フック登録

- =find-file-hook= → =completion-lsp-on-project-switch)=
- =project-switch-project-hook= → =completion-lsp-on-project-switch)=

*** defcustom 変数

- =completion-lsp-enable-p=

*** 内部関数

- =completion-lsp--project-root=
  現在のバッファのプロジェクトルートを =project-current= から取得する。=eglot--project= が利用可能な場合は =fboundp= ガード下で使用する。
- =completion-lsp--shutdown-stale-servers=
  新しいプロジェクトルートと一致しない既存の Eglot サーバーを =eglot-shutdown= で停止する。

* 7. orgx/ Native Compilation 警告の収集・管理・診断 Org エコシステム
:PROPERTIES:
:CUSTOM_ID: layer-orgx
:END:

| # | モジュール | 責務 | pub | cust | hook |
|---|-----------|------|-----|------|------|
| 41 | =orgx-core= | Org Mode コア設定とディレクトリレイアウト（2フェーズ agenda） | 1 | 0 | 1 |
| 42 | =orgx-visual= | org-modern・pretty entities・カスタムフェイスによる視覚強化 | 0 | 0 | 2 |
| 43 | =orgx-extensions= | org-journal/roam/download/toc/cliplink の設定 | 0 | 0 | 0 |
| 44 | =orgx-fold= | Emacs バージョン互換の追加フォールドコマンド | 3 | 0 | 0 |
| 45 | =orgx-export= | Hugo/Markdown/Mermaid/Graphviz エクスポートバックエンド | 0 | 0 | 0 |
| 46 | =orgx-notes-markdown= | Org ディレクトリ配下の軽量 Markdown ノートシステム | 6 | 2 | 1 |
| 47 | =orgx-auto-tangle= | README.org 保存時の org-babel-tangle 自動実行 | 0 | 0 | 2 |

** orgx/orgx-core.el
:PROPERTIES:
:CUSTOM_ID: orgx-core
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #41 |
| tangle 先 | =lisp/orgx/orgx-core.el= |
| provide | =orgx-core= |

*** 責務

Org Mode のコア設定とディレクトリレイアウトを確立する。
=org-agenda-files= を2フェーズで設定する：フェーズ1で最小フォールバック（inbox.org）を即時設定し、
=after-init-hook= でフェーズ2として =utils-org-agenda-build= を呼び出してキャッシュ済みの完全リストに置き換える。
これにより orgx（org 層）が utils 層に起動時直接依存しないレイヤー分離を維持する（Fix 2）。
=cl-lib= を実行時に =require= し、インタープリタ実行時にも =cl-loop= を利用可能にする（Fix C）。
=org-agenda-files=・=org-todo-keywords=・=org-refile-targets=・=org-capture-templates= は
backquote 展開や関数参照を含む runtime 評価値のため =setq= を使用する（Fix AA）。

*** 公開 API

- =my/org-buffer-files=

*** フック登録

- =after-init-hook= → =orgx-core--rebuild-agenda-files)=

*** 内部関数

- =orgx-core--rebuild-agenda-files=
  =after-init-hook= から呼ばれる（フェーズ2）。=utils-org-agenda-build= が =fboundp= の場合に呼び出し、返されたファイルリストで =org-agenda-files= を =setq= で更新する。フェーズ1のフォールバック値を完全なスキャン結果で置き換える。

** orgx/orgx-visual.el
:PROPERTIES:
:CUSTOM_ID: orgx-visual
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #42 |
| tangle 先 | =lisp/orgx/orgx-visual.el= |
| provide | =orgx-visual= |

*** 責務

Org Mode の視覚的強化を設定する。ガードフラグにより =after-init-hook= で正確に1回のみ
=org-modern= の視覚変数（見出し星・タグ配置・強調マーカー・pretty entities 等）を設定する。
カスタム TODO キーワードフェイスとリスト/チェックボックスシンボルを定義し、
=org-mode-hook= でバッファごとに =org-modern-mode= を有効化する。

*** フック登録

- =after-init-hook= → =orgx-visual--configure-once)=
- =org-mode-hook= → =orgx-visual--enable-mode)=

*** 内部関数

- =orgx-visual--configure-once=
  =after-init-hook= から呼ばれる。=orgx-visual--initialized= フラグを確認し、未実行の場合のみ =org-modern= 変数の設定とフェイス定義を行う。フラグを =t= にセットして再実行を防ぐ。
- =orgx-visual--enable-mode=
  =org-mode-hook= から呼ばれる。バッファローカルで =org-modern-mode= を有効化する。

** orgx/orgx-extensions.el
:PROPERTIES:
:CUSTOM_ID: orgx-extensions
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #43 |
| tangle 先 | =lisp/orgx/orgx-extensions.el= |
| provide | =orgx-extensions= |

*** 責務

Org 拡張パッケージ群を設定する。=org-journal= を =my:d:org-journal= で設定し、
=org-roam= を =my:d:org-roam= 配下に DB を配置してアイドルタイマーで =org-roam-db-autosync-mode= を遅延起動する。
org-roam の起動失敗は =condition-case= で隔離する。=org-download=・=toc-org=・=org-cliplink= もインストールする。

*** 内部関数

- =orgx--org-roam-file-hash-guard=
  org-roam の DB 更新を間引くためのハッシュベースのガード。同一ファイルの連続保存で DB 更新が重複しないよう制御する。
- =orgx--org-roam-autosync-enable=
  アイドルタイマーから呼ばれる。=condition-case= で =org-roam-db-autosync-mode= の有効化を試み、失敗は =message= に記録して起動を止めない。

** orgx/orgx-fold.el
:PROPERTIES:
:CUSTOM_ID: orgx-fold
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #44 |
| tangle 先 | =lisp/orgx/orgx-fold.el= |
| provide | =orgx-fold= |

*** 責務

Emacs バージョン互換性を持つ追加 Org フォールドコマンドを提供する。
=my/org-fold-subtree=・=my/org-unfold-subtree=・=my/org-toggle-fold= を
インタラクティブコマンドとして =C-c f=・=C-c e=・=C-c t= に =org-mode-map= でバインドする。
=org-fold-folded-p= を =fboundp= でガードし、Emacs 29 未満では =outline-invisible-p= にフォールバックする。

*** 公開 API

- =my/org-fold-subtree=
- =my/org-unfold-subtree=
- =my/org-toggle-fold=

** orgx/orgx-export.el
:PROPERTIES:
:CUSTOM_ID: orgx-export
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #45 |
| tangle 先 | =lisp/orgx/orgx-export.el= |
| provide | =orgx-export= |

*** 責務

Org エクスポートバックエンドを設定する。=ox-hugo= を TOML フロントマターで Hugo ブログエクスポート用に設定し、
=markdown-mode=・=markdown-preview-mode=・=edit-indirect= をインストールする。
=mmdc= CLI が利用可能な場合は =ob-mermaid= を設定し、Graphviz ダイアグラム用に =ob-dot= を有効化する。

** orgx/orgx-notes-markdown.el
:PROPERTIES:
:CUSTOM_ID: orgx-notes-markdown
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #46 |
| tangle 先 | =lisp/orgx/orgx-notes-markdown.el= |
| defgroup | =my:notes= |
| provide | =orgx-notes-markdown= |

*** 責務

Org ディレクトリ配下に軽量な Markdown ノートシステムを提供する。
ノートブック形式のディレクトリ構造・タイムスタンプ付き Markdown ノート作成・
Pandoc ベースの編集・consult-ripgrep によるノート横断検索・画像用 org-download 統合を含む。
複雑な知識グラフフレームワークを避け、意図的に最小限の設計にしている。

*** 公開 API

- =my/notes-new-note=
- =my/notes-open-root=
- =my/markdown-toggle-live-preview=
- =my/notes-markdown-visual-fill=
- =my/notes-consult-ripgrep=
- =my/notes-markdown-org-download-setup=

*** フック登録

- =markdown-mode-hook= → =my/notes-markdown-org-download-setup))=

*** defcustom 変数

- =my:notes-directory-name=
- =my:notes-default-notebooks=

*** 内部関数

- =orgx-notes--ensure-root=
  ノートルートディレクトリ（=my:notes-directory-name= が示すパス）が存在しない場合に =make-directory= で作成する。
- =orgx-notes--slugify=
  タイトル文字列をファイル名に適した slug（小文字・スペースをハイフンに変換・記号除去）に変換する。

** orgx/orgx-auto-tangle.el
:PROPERTIES:
:CUSTOM_ID: orgx-auto-tangle
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #47 |
| tangle 先 | =lisp/orgx/orgx-auto-tangle.el= |
| provide | =orgx-auto-tangle= |

*** 責務

=README.org= 保存時に =org-babel-tangle= を自動実行する。
=my/orgx-auto-tangle-target-file=（"README.org"）に一致するファイルのみを対象とし、
バッファローカルな進行中フラグで再帰的保存ループを防止する。
=org-mode-hook= で =after-save-hook= に named フック関数を登録する。

*** フック登録

- =after-save-hook= → =orgx-auto-tangle--maybe=
- =org-mode-hook= → =orgx-auto-tangle--install-hook)=

*** 内部関数

- =orgx-auto-tangle--eligible-p=
  現在のバッファが自動タングルの対象（ファイル名が =README.org= に一致し、かつ進行中フラグが nil）かを判定する。
- =orgx-auto-tangle--maybe=
  =after-save-hook= から呼ばれる。=orgx-auto-tangle--eligible-p= が t の場合のみ進行中フラグを立てて =org-babel-tangle= を呼び出す。完了後フラグを下ろす。
- =orgx-auto-tangle--install-hook=
  =org-mode-hook= から呼ばれる。バッファローカルで =after-save-hook= に =orgx-auto-tangle--maybe= を追加する。

* 8. vcs/ Native Compilation 警告の収集・管理・診断 バージョン管理
:PROPERTIES:
:CUSTOM_ID: layer-vcs
:END:

| # | モジュール | 責務 | pub | cust | hook |
|---|-----------|------|-----|------|------|
| 48 | =vcs-magit= | Magit Git クライアントの設定 | 0 | 0 | 0 |
| 49 | =vcs-gutter= | diff-hl による VCS ガターインジケーター | 0 | 0 | 0 |
| 50 | =vcs-forge= | GitHub/GitLab Issue/PR 向け Magit Forge 統合 | 0 | 0 | 0 |

** vcs/vcs-magit.el
:PROPERTIES:
:CUSTOM_ID: vcs-magit
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #48 |
| tangle 先 | =lisp/vcs/vcs-magit.el= |
| provide | =vcs-magit= |

*** 責務

Magit Git クライアントを設定する。=magit-display-buffer-fullframe-status-v1= で
フルフレームステータス表示を有効化し、Magit バッファを bury したときに
直前のウィンドウ構成を自動復元する。

** vcs/vcs-gutter.el
:PROPERTIES:
:CUSTOM_ID: vcs-gutter
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #49 |
| tangle 先 | =lisp/vcs/vcs-gutter.el= |
| provide | =vcs-gutter= |

*** 責務

diff-hl による VCS ガターインジケーターを設定する。
初期化後に =global-diff-hl-mode= を有効化し、magit の =magit-pre-refresh-hook= と
=magit-post-refresh-hook= でフリンジインジケーターを再描画する。

** vcs/vcs-forge.el
:PROPERTIES:
:CUSTOM_ID: vcs-forge
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #50 |
| tangle 先 | =lisp/vcs/vcs-forge.el= |
| provide | =vcs-forge= |

*** 責務

GitHub/GitLab の Issue と PR を管理する Magit Forge 統合を設定する。
=with-eval-after-load 'magit= で magit が利用可能になった後に forge をロードする。

* 9. dev/ Native Compilation 警告の収集・管理・診断 開発ツール
:PROPERTIES:
:CUSTOM_ID: layer-dev
:END:

| # | モジュール | 責務 | pub | cust | hook |
|---|-----------|------|-----|------|------|
| 51 | =dev-lsp-bridge= | LSP バックエンド非依存の統一コードアクション・リネーム・フォーマット | 3 | 0 | 0 |
| 52 | =dev-ai= | aidermacs による AI 支援開発環境の設定 | 0 | 0 | 0 |
| 53 | =dev-term= | vterm ベースのターミナル統合ヘルパー | 1 | 1 | 2 |
| 54 | =dev-web-core= | project.el・ファイル衛生・EditorConfig の設定 | 0 | 0 | 0 |
| 55 | =dev-build= | Makefile 編集・コンパイルバッファ・ANSI カラーの設定 | 1 | 0 | 0 |
| 56 | =dev-format= | Apheleia による非同期自動フォーマット | 0 | 0 | 0 |
| 57 | =dev-infra-modes= | インフラ・設定ファイルへのメジャーモード割り当て | 0 | 0 | 0 |
| 58 | =dev-docker= | Dockerfile/YAML/docker CLI/TRAMP コンテナ統合 | 0 | 0 | 0 |
| 59 | =dev-music= | EMMS による Emacs 内音楽再生統合 | 0 | 1 | 1 |
| 60 | =dev-sql= | PostgreSQL 向け SQL 編集・フォーマット設定 | 0 | 0 | 0 |
| 61 | =dev-rest= | restclient・jq 統合による HTTP 開発環境 | 0 | 0 | 0 |
| 62 | =dev-navigation= | dumb-jump・multiple-cursors・EWW によるコードナビゲーション | 4 | 0 | 3 |
| 63 | =dev-tools= | 開発用雑多なインタラクティブコマンド | 3 | 0 | 0 |

** dev/dev-lsp-bridge.el
:PROPERTIES:
:CUSTOM_ID: dev-lsp-bridge
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #51 |
| tangle 先 | =lisp/dev/dev-lsp-bridge.el= |
| provide | =dev-lsp-bridge= |

*** 責務

LSP バックエンドに依存しない統一コードアクション・リネーム・フォーマットコマンドを提供する。
=dev-lsp-code-actions=・=dev-lsp-rename=・=dev-lsp-format-buffer= を呼び出し時に
=cond= / =fboundp= で eglot または lsp-mode にディスパッチし、LSP 未使用時は =indent-region= にフォールバックする。
dev/ セクションの先頭（#51）にロードされることで、ui-keymap や personal オーバーレイが
バックエンドの有効化前から =dev-lsp-*= コマンドをバインドできる。

*** 公開 API

- =dev-lsp-code-actions=
- =dev-lsp-rename=
- =dev-lsp-format-buffer=

** dev/dev-ai.el
:PROPERTIES:
:CUSTOM_ID: dev-ai
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #52 |
| tangle 先 | =lisp/dev/dev-ai.el= |
| provide | =dev-ai= |

*** 責務

aidermacs（aider.chat の Emacs フロントエンド）による AI 支援開発環境を設定する。
履歴とログファイルを =my:d:aider= 配下に配置し、=dev-ai--append-unique-args= で
重複なく CLI 引数を追加する。vterm が利用可能な場合は vterm を、そうでなければ comint を
ターミナルバックエンドとして選択する。=OPENROUTER_API_KEY= が設定されている場合は OpenRouter 経由、
なければ =OPENAI_API_KEY= で直接 OpenAI にアクセスする。

*** 内部関数

- =dev-ai--append-unique-args=
  =aidermacs-extra-args= リストに引数を追加する際、すでに同じ引数が含まれていない場合のみ追加する。=member= で存在確認を行うことで起動のたびに引数が重複するのを防ぐ。

** dev/dev-term.el
:PROPERTIES:
:CUSTOM_ID: dev-term
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #53 |
| tangle 先 | =lisp/dev/dev-term.el= |
| defgroup | =dev-term= |
| provide | =dev-term= |

*** 責務

vterm ベースのターミナル統合ヘルパーを提供する。Nano パレット統合（=ui-nano-palette= が
ロードされている場合のみ）・vterm-toggle によるプロジェクトスコープのターミナルトグル・
ボトムドッキング型ウィンドウ表示を設定する。=dev-term--nano-color= は =ui-nano-palette= が
未ロードの場合に安全なフォールバックカラーを返すため、このモジュールは起動時に
=ui-nano-palette= を =require= しない（レイヤー独立性の維持）。

*** 公開 API

- =dev-term-apply-palette=

*** フック登録

- =enable-theme-functions= → =dev-term--reapply-palette)=
- =after-make-frame-functions= → =dev-term--frame-init))=

*** defcustom 変数

- =dev-term-enable-p=

*** 内部関数

- =dev-term--nano-color=
  Nano パレットの色変数（例: =nano-color-background=）を =bound-and-true-p= で安全に読み取る。未定義の場合はフォールバック値を返す。
- =dev-term--safe-face-set=
  指定フェイスが存在する場合のみ =set-face-attribute= を呼び出す安全ラッパー。
- =dev-term--buffer-p=
  指定バッファが vterm バッファかを判定する述語。
- =dev-term--reapply-palette=
  =enable-theme-functions= から呼ばれる。テーマ切り替え後に =dev-term-apply-palette= を再実行してターミナルフェイスを更新する。
- =dev-term--frame-init=
  =after-make-frame-functions= から呼ばれる。新規フレームでターミナルフェイスを初期化する。
- =dev-term--display-buffer-p=
  vterm バッファのウィンドウ表示設定（ボトムドッキング）を制御する述語。=display-buffer-alist= から参照される。

** dev/dev-web-core.el
:PROPERTIES:
:CUSTOM_ID: dev-web-core
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #54 |
| tangle 先 | =lisp/dev/dev-web-core.el= |
| provide | =dev-web-core= |

*** 責務

project.el・ファイル衛生・EditorConfig の設定 =project.el= によるプロジェクト対応コマンドを設定し、保存時の最終改行確保と末尾空白除去を適用し、=editorconfig-mode= をグローバルに有効化してプロジェクト別スタイルを適用する。

** dev/dev-build.el
:PROPERTIES:
:CUSTOM_ID: dev-build
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #55 |
| tangle 先 | =lisp/dev/dev-build.el= |
| provide | =dev-build= |

*** 責務

Makefile 編集・コンパイルバッファ・ANSI カラーを設定する。
Makefile バリアントを =makefile-gmake-mode= にマッピングし、タブインデントと
末尾空白の可視化を強制する。コンパイルバッファの ANSI カラー出力を =ansi-color-apply-on-region= で着色し、
Makefile バッファでの =compile-command= を ="make -k"= に設定する。

*** 公開 API

- =my/set-make-compile-command=

*** 内部関数

- =dev-build--makefile-setup=
  =makefile-mode-hook= 系フックから呼ばれる。タブ幅・=require-final-newline=・末尾空白可視化・=compile-command= を設定する。
- =dev-build--ansi-color-compilation=
  =compilation-filter-hook= から呼ばれる。コンパイルバッファの新着テキスト領域に =ansi-color-apply-on-region= を適用して ANSI エスケープを色に変換する。

** dev/dev-format.el
:PROPERTIES:
:CUSTOM_ID: dev-format
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #56 |
| tangle 先 | =lisp/dev/dev-format.el= |
| provide | =dev-format= |

*** 責務

Apheleia による非同期自動フォーマット prettierd と prettier を Apheleia フォーマッターとして登録し、TypeScript・TSX・JSON・CSS・Markdown モードを prettierd にマッピングする。=apheleia-global-mode= で保存時自動フォーマットをグローバルに有効化する。

** dev/dev-infra-modes.el
:PROPERTIES:
:CUSTOM_ID: dev-infra-modes
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #57 |
| tangle 先 | =lisp/dev/dev-infra-modes.el= |
| provide | =dev-infra-modes= |

*** 責務

インフラ・設定ファイルへのメジャーモード割り当て docker-compose YAML → =docker-compose-mode=、=.env= / =.env.*= → =dotenv-mode=、=.toml= → =toml-mode= にメジャーモードを割り当てる。

** dev/dev-docker.el
:PROPERTIES:
:CUSTOM_ID: dev-docker
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #58 |
| tangle 先 | =lisp/dev/dev-docker.el= |
| provide | =dev-docker= |

*** 責務

Dockerfile/YAML/docker CLI/TRAMP コンテナ統合 dockerfile-mode（Dockerfile / .dockerfile）・yaml-mode（docker-compose / 汎用 YAML）を設定し、=docker= トランジェントでコンテナ/イメージ/ボリューム管理を提供する。=tramp-container= で =docker:= リモートホストアクセスを設定し、tempel の Dockerfile スニペットテンプレートを登録する。

** dev/dev-music.el
:PROPERTIES:
:CUSTOM_ID: dev-music
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #59 |
| tangle 先 | =lisp/dev/dev-music.el= |
| defgroup | =dev-music= |
| provide | =dev-music= |

*** 責務

EMMS（Emacs Multimedia System）による Emacs 内フル機能音楽プレーヤーを設定する。
=mpv=（優先）または =mplayer= による外部音声再生・プレイリスト管理・Dired 統合・
ID3 メタデータ抽出・モードライン再生状態表示・ディレクトリベースの音楽ライブラリをサポートする。
コアレイヤーに干渉せず、デーモン・GUI・ターミナル環境で安全に動作する。外部依存: mpv（=brew install mpv=）。

*** フック登録

- =dired-mode-hook= → =dev-music--setup-dired))=

*** defcustom 変数

- =dev-music-enable-p=

*** 内部関数

- =dev-music--setup-dired=
  =dired-mode-hook= から呼ばれる。EMMS の Dired 統合キーバインド（=RET= でファイル再生等）をバッファローカルで設定する。

** dev/dev-sql.el
:PROPERTIES:
:CUSTOM_ID: dev-sql
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #60 |
| tangle 先 | =lisp/dev/dev-sql.el= |
| provide | =dev-sql= |

*** 責務

PostgreSQL 向け SQL 編集・フォーマット設定 デフォルト SQL 製品を =postgres= に設定し、sql-mode バッファで =sqlind-minor-mode= を有効化する。=pgformatter= による保存時自動フォーマットを設定する。

** dev/dev-rest.el
:PROPERTIES:
:CUSTOM_ID: dev-rest
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #61 |
| tangle 先 | =lisp/dev/dev-rest.el= |
| provide | =dev-rest= |

*** 責務

restclient・jq 統合による HTTP 開発環境 =.http= ファイルを =restclient-mode= に関連付け、jq によるレスポンスフィルタリングのために restclient の後に =restclient-jq= をロードする。

** dev/dev-navigation.el
:PROPERTIES:
:CUSTOM_ID: dev-navigation
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #62 |
| tangle 先 | =lisp/dev/dev-navigation.el= |
| provide | =dev-navigation= |

*** 責務

dumb-jump・multiple-cursors・EWW によるコードナビゲーションヘルパーを提供する。
=dumb-jump= を eglot が管理していないバッファのセカンダリ xref バックエンドとして登録し、
ripgrep を強制的に使用する。=multiple-cursors= をインストールし、
=my/eww-search= と =my/eww-toggle-images= で EWW ブラウジングを補助し、
レンダリング後のページで検索語をハイライトする。

*** 公開 API

- =my/dumb-jump-backend=
- =my/eww-after-render=
- =my/eww-search=
- =my/eww-toggle-images=

*** フック登録

- =xref-backend-functions= → =my/dumb-jump-backend=
- =prog-mode-hook= → =dev-navigation--dumb-jump-setup))=
- =eww-after-render-hook= → =my/eww-after-render))=

*** 内部関数

- =dev-navigation--dumb-jump-setup=
  =prog-mode-hook= から呼ばれる。=eglot-managed-p= を確認し、eglot 非管理のバッファのみ =xref-backend-functions= に =my/dumb-jump-backend= を追加する。

** dev/dev-tools.el
:PROPERTIES:
:CUSTOM_ID: dev-tools
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #63 |
| tangle 先 | =lisp/dev/dev-tools.el= |
| provide | =dev-tools= |

*** 責務

開発用の雑多なインタラクティブユーティリティコマンドを提供する。
=my/open-by-vscode= で現在ファイルを行・列指定で VS Code に開き（=code -r -g=）、
=my/show-env-variable= で環境変数の値を表示し、
=my/print-build-info= で Emacs ビルドメタデータを =*Build Info*= バッファに Markdown テーブルで出力する。

*** 公開 API

- =my/open-by-vscode=
- =my/show-env-variable=
- =my/print-build-info=

* 10. utils/ Native Compilation 警告の収集・管理・診断 汎用ヘルパー
:PROPERTIES:
:CUSTOM_ID: layer-utils
:END:

| # | モジュール | 責務 | pub | cust | hook |
|---|-----------|------|-----|------|------|
| 64 | =utils-path= | 設定モジュール向けパス操作ヘルパー | 3 | 0 | 0 |
| 65 | =utils-async= | 非ブロッキングサブプロセス実行ユーティリティ | 2 | 0 | 0 |
| 66 | =utils-buffers= | オプション保存プロンプト付きスマートバッファ kill | 1 | 0 | 0 |
| 67 | =utils-edit= | テキスト編集利便コマンド（リインデント・コピー・行結合） | 3 | 0 | 0 |
| 68 | =utils-dired= | Dired のスマートナビゲーション（dirvish-find-file 使用） | 1 | 0 | 0 |
| 69 | =utils-functions= | 設定管理・デバッグ向け雑多なインタラクティブコマンド | 4 | 0 | 0 |
| 70 | =utils-org-agenda= | org-directory スキャン・キャッシュによる agenda ファイルリスト提供 | 2 | 3 | 0 |
| 71 | =utils-lint= | Flycheck/Flymake 診断とサイドライン表示 | 0 | 0 | 0 |
| 72 | =utils-diagnostics= | 設定環境のトラブルシューティング向け診断コマンド | 3 | 0 | 0 |

** utils/utils-path.el
:PROPERTIES:
:CUSTOM_ID: utils-path
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #64 |
| tangle 先 | =lisp/utils/utils-path.el= |
| provide | =utils-path= |

*** 責務

設定モジュールが使用するパス操作ヘルパーを提供する。
=utils-path-normalize= で末尾スラッシュ付きの正規化パスを返し、
=utils-path-relative= で子パスをベースディレクトリからの相対パスに変換（配下でない場合は nil）し、
=utils-path-safe-name= で英数字以外をハイフンに置換した安全なファイル名を返す。
=my/ensure-directory-exists= は early-init.el 定義済みのためここでは参照のみ。

*** 公開 API

- =utils-path-normalize=
- =utils-path-relative=
- =utils-path-safe-name=

** utils/utils-async.el
:PROPERTIES:
:CUSTOM_ID: utils-async
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #65 |
| tangle 先 | =lisp/utils/utils-async.el= |
| provide | =utils-async= |

*** 責務

非ブロッキングサブプロセス実行のためのユーティリティを提供する。
=utils-async-shell-command= で指定バッファ名にコマンド出力を非同期に書き込み、
=utils-async-run-script= で =file-executable-p= ガード付きの実行ファイルをオプション引数付きで起動する。

*** 公開 API

- =utils-async-shell-command=
- =utils-async-run-script=

** utils/utils-buffers.el
:PROPERTIES:
:CUSTOM_ID: utils-buffers
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #66 |
| tangle 先 | =lisp/utils/utils-buffers.el= |
| provide | =utils-buffers= |

*** 責務

=my/smart-kill-buffer= を提供する。変更済みファイルバッファに対して保存を促すプロンプトを表示し、
ユーザーの選択に関わらず最終的にバッファを kill する。通常の =kill-buffer= より安全な代替。

*** 公開 API

- =my/smart-kill-buffer=

** utils/utils-edit.el
:PROPERTIES:
:CUSTOM_ID: utils-edit
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #67 |
| tangle 先 | =lisp/utils/utils-edit.el= |
| provide | =utils-edit= |

*** 責務

テキスト編集利便コマンドを3つ提供する。
=my/indent-buffer= でバッファ全体をリインデントし、
=my/copy-whole-buffer= でバッファ全内容をキルリングにコピーし、
=my/join-next-line= で次行を現在行の末尾に結合する。

*** 公開 API

- =my/indent-buffer=
- =my/copy-whole-buffer=
- =my/join-next-line=

** utils/utils-dired.el
:PROPERTIES:
:CUSTOM_ID: utils-dired
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #68 |
| tangle 先 | =lisp/utils/utils-dired.el= |
| provide | =utils-dired= |

*** 責務

Dired の =RET= で賢くファイルまたはディレクトリをナビゲートする。
ディレクトリは =dirvish-find-file= で既存 dirvish セッション内でナビゲートし、
ファイルは =find-file= で開く。=dirvish= 関数（新規フルフレームセッションを作成）の誤用を避けるための修正
（=dirvish-find-file= が正しい API）。dirvish 未ロード時は =dired-find-file= にフォールバックする。

*** 公開 API

- =my/dired-get-file-or-dir=

** utils/utils-functions.el
:PROPERTIES:
:CUSTOM_ID: utils-functions
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #69 |
| tangle 先 | =lisp/utils/utils-functions.el= |
| provide | =utils-functions= |

*** 責務

設定管理・デバッグ向けの雑多なインタラクティブコマンドを提供する。
=my/reload-config= で =init.el= を再ロードし、=my/open-emacs-config= で =README.org= を開き、
=my/show-full-path= で現在バッファの絶対パスをエコーエリアに表示し、
=my/toggle-debug-on-error= で =debug-on-error= をトグルして状態を報告する。

*** 公開 API

- =my/reload-config=
- =my/open-emacs-config=
- =my/show-full-path=
- =my/toggle-debug-on-error=

** utils/utils-org-agenda.el
:PROPERTIES:
:CUSTOM_ID: utils-org-agenda
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #70 |
| tangle 先 | =lisp/utils/utils-org-agenda.el= |
| defgroup | =utils-org-agenda= |
| provide | =utils-org-agenda= |

*** 責務

=org-directory= を1回スキャンして結果をキャッシュし、=org-agenda-files= 用のファイルリストを返す。
=orgx-core= から =after-init-hook= 経由で呼ばれることで、utils 層への依存を org 層のロード時から
起動後に後退させ、レイヤー分離を維持する。=utils-org-agenda-invalidate-cache= でキャッシュを強制無効化できる。

*** 公開 API

- =utils-org-agenda-invalidate-cache=
- =utils-org-agenda-build=

*** defcustom 変数

- =utils-org-agenda-max-depth=
- =utils-org-agenda-exclude-dirs=
- =utils-org-agenda-cache-ttl=

*** 内部関数

- =utils-org-agenda--should-exclude-p=
  引数のパスが =utils-org-agenda-exclude-dirs= リストに含まれるか確認し、スキャン対象から除外すべきかを返す。
- =utils-org-agenda--scan=
  =org-directory= を再帰的にスキャンし、=utils-org-agenda-max-depth= の深さまで =.org= ファイルを収集してリストで返す。除外ディレクトリは =utils-org-agenda--should-exclude-p= でフィルタリングする。

** utils/utils-lint.el
:PROPERTIES:
:CUSTOM_ID: utils-lint
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #71 |
| tangle 先 | =lisp/utils/utils-lint.el= |
| provide | =utils-lint= |

*** 責務

Flycheck と Flymake による診断とサイドライン表示を設定する。
Flycheck をオンデマンドチェック用にインストールし、全 =prog-mode= バッファで Flymake を有効化する。
=sideline= で Flycheck/Flymake のメッセージをバッファ右端に表示する。

** utils/utils-diagnostics.el
:PROPERTIES:
:CUSTOM_ID: utils-diagnostics
:END:

| 項目 | 内容 |
|------|------|
| ロード順 | #72 |
| tangle 先 | =lisp/utils/utils-diagnostics.el= |
| provide | =utils-diagnostics= |

*** 責務

設定環境のトラブルシューティング向け診断コマンドを提供する。
=my/print-load-path= で =load-path= を専用バッファに表示し、
=my/print-feature-list= でロード済みフィーチャをアルファベット順に一覧表示し、
=my/check-missing-executables= で =rg=・=git=・=make=・=aspell=・=gpg=・=pass= の不在をレポートする。

*** 公開 API

- =my/print-load-path=
- =my/print-feature-list=
- =my/check-missing-executables=

* 付録 A: モジュールインデックス
:PROPERTIES:
:CUSTOM_ID: appendix-a
:END:

ロード順・レイヤー・公開 defun 数・defcustom 数・フック数

| # | モジュール | レイヤー | pub | cust | hook |
|---|-----------|---------|-----|------|------|
| 1 | =core-fixes= | core | 0 | 0 | 0 |
| 2 | =core-policy= | core | 1 | 0 | 1 |
| 3 | =core-session= | core | 5 | 3 | 0 |
| 4 | =core-session-private= | core | 0 | 0 | 0 |
| 5 | =core-gc= | core | 0 | 1 | 2 |
| 6 | =core-persistence= | core | 1 | 0 | 1 |
| 7 | =core-buffers= | core | 2 | 0 | 1 |
| 8 | =core-runtime= | core | 0 | 1 | 1 |
| 9 | =core-tools= | core | 7 | 0 | 0 |
| 10 | =core-treesit= | core | 5 | 3 | 1 |
| 11 | =core-history= | core | 0 | 0 | 1 |
| 12 | =core-editing= | core | 0 | 0 | 0 |
| 13 | =core-switches= | core | 0 | 2 | 1 |
| 14 | =core-custom= | core | 2 | 0 | 0 |
| 15 | =core-api= | core | 3 | 0 | 0 |
| 16 | =core-native= | core | 0 | 0 | 0 |
| 17 | =ui-font= | ui | 3 | 4 | 2 |
| 18 | =ui-theme= | ui | 0 | 2 | 3 |
| 19 | =ui-window= | ui | 2 | 0 | 0 |
| 20 | =ui-utils= | ui | 0 | 0 | 0 |
| 21 | =ui-health-modeline= | ui | 0 | 3 | 4 |
| 22 | =ui-imenu= | ui | 0 | 0 | 0 |
| 23 | =ui-leader= | ui | 1 | 3 | 0 |
| 24 | =ui-hydra= | ui | 2 | 1 | 0 |
| 25 | =ui-which-key= | ui | 0 | 2 | 1 |
| 26 | =ui-keymap= | ui | 0 | 1 | 1 |
| 27 | =ui-global-keys= | ui | 0 | 0 | 1 |
| 28 | =auth-core= | auth | 1 | 1 | 1 |
| 29 | =completion-core= | completion | 0 | 0 | 0 |
| 30 | =completion-vertico= | completion | 0 | 0 | 1 |
| 31 | =completion-consult= | completion | 0 | 0 | 0 |
| 32 | =completion-embark= | completion | 0 | 0 | 0 |
| 33 | =completion-corfu= | completion | 0 | 0 | 1 |
| 34 | =completion-icons= | completion | 0 | 0 | 0 |
| 35 | =completion-capf= | completion | 0 | 0 | 9 |
| 36 | =completion-capf-org-src= | completion | 0 | 0 | 1 |
| 37 | =completion-capf-org-src-lang= | completion | 0 | 0 | 1 |
| 38 | =completion-corfu-org-src= | completion | 0 | 0 | 1 |
| 39 | =completion-orderless-org-src= | completion | 0 | 0 | 1 |
| 40 | =completion-lsp= | completion | 1 | 1 | 2 |
| 41 | =orgx-core= | orgx | 1 | 0 | 1 |
| 42 | =orgx-visual= | orgx | 0 | 0 | 2 |
| 43 | =orgx-extensions= | orgx | 0 | 0 | 0 |
| 44 | =orgx-fold= | orgx | 3 | 0 | 0 |
| 45 | =orgx-export= | orgx | 0 | 0 | 0 |
| 46 | =orgx-notes-markdown= | orgx | 6 | 2 | 1 |
| 47 | =orgx-auto-tangle= | orgx | 0 | 0 | 2 |
| 48 | =vcs-magit= | vcs | 0 | 0 | 0 |
| 49 | =vcs-gutter= | vcs | 0 | 0 | 0 |
| 50 | =vcs-forge= | vcs | 0 | 0 | 0 |
| 51 | =dev-lsp-bridge= | dev | 3 | 0 | 0 |
| 52 | =dev-ai= | dev | 0 | 0 | 0 |
| 53 | =dev-term= | dev | 1 | 1 | 2 |
| 54 | =dev-web-core= | dev | 0 | 0 | 0 |
| 55 | =dev-build= | dev | 1 | 0 | 0 |
| 56 | =dev-format= | dev | 0 | 0 | 0 |
| 57 | =dev-infra-modes= | dev | 0 | 0 | 0 |
| 58 | =dev-docker= | dev | 0 | 0 | 0 |
| 59 | =dev-music= | dev | 0 | 1 | 1 |
| 60 | =dev-sql= | dev | 0 | 0 | 0 |
| 61 | =dev-rest= | dev | 0 | 0 | 0 |
| 62 | =dev-navigation= | dev | 4 | 0 | 3 |
| 63 | =dev-tools= | dev | 3 | 0 | 0 |
| 64 | =utils-path= | utils | 3 | 0 | 0 |
| 65 | =utils-async= | utils | 2 | 0 | 0 |
| 66 | =utils-buffers= | utils | 1 | 0 | 0 |
| 67 | =utils-edit= | utils | 3 | 0 | 0 |
| 68 | =utils-dired= | utils | 1 | 0 | 0 |
| 69 | =utils-functions= | utils | 4 | 0 | 0 |
| 70 | =utils-org-agenda= | utils | 2 | 3 | 0 |
| 71 | =utils-lint= | utils | 0 | 0 | 0 |
| 72 | =utils-diagnostics= | utils | 3 | 0 | 0 |
| | *合計 72 モジュール* | | *93* | *57* | *59* |

* 付録 B: 命名規則リファレンス
:PROPERTIES:
:CUSTOM_ID: appendix-b
:END:

| パターン | 例 | 用途 |
|----------|-----|------|
| =my/<n>= | =my/safe-load-file= | 公開インタラクティブコマンド・クロスモジュール公開関数 |
| =<module>-<n>= | =core-tools-emacs>=, =ui-font-apply= | モジュール公開 API（モジュールプレフィックス付き） |
| =<module>--<n>= | =core-custom--ensure-file=, =modules--require-safe= | モジュール内部ヘルパー（double dash = private）。=my/<topic>--= 形式は禁止。 |
| =my:d:*= | =my:d:var=, =my:d:org=, =my:d:treesit= | ディレクトリ / パス変数 |
| =<group>-<n>= | =core-session-enable-p=, =ui-font-size= | defcustom 変数（defgroup 名と一致） |
| =bootstrap--*= | =bootstrap--safe-load-file= | init.el 内部ヘルパー（modules.el ロード前） |
| =<module>-enable-p= | =core-gc-enable-p=, =dev-term-enable-p= | 機能の有効/無効フラグ defcustom |
| =<module>-*-p= | =core-treesit-enable-p=, =ui-hydra-enable-p= | ブール述語 defcustom |

* 付録 C: 主要公開 API 早見表
:PROPERTIES:
:CUSTOM_ID: appendix-c
:END:

** コア API（常に利用可能）

| 関数 | 所在モジュール | 用途 |
|------|--------------|------|
| =my/ensure-directory-exists= | early-init.el | ディレクトリ作成（起動時から利用可） |
| =core-api-session-register-job= | core-api | セッションジョブ登録ファサード |
| =core-api-session-unregister-job= | core-api | セッションジョブ登録解除ファサード |
| =core-api-session-run-job= | core-api | セッションジョブ即時実行ファサード |
| =core-treesit-install-all= | core-treesit | 全 Tree-sitter 文法インストール |
| =core-treesit-report= | core-treesit | Tree-sitter 状態レポート |
| =my/custom-open= | core-custom | custom-file を編集用に開く |

** LSP 統一 API

| 関数 | 用途 |
|------|------|
| =dev-lsp-code-actions= | eglot / lsp-mode 両対応コードアクション |
| =dev-lsp-rename= | eglot / lsp-mode 両対応シンボルリネーム |
| =dev-lsp-format-buffer= | eglot / lsp-mode 両対応バッファフォーマット |

** Native Compilation 診断 API

| 関数 | 用途 |
|------|------|
| =core-native-clear-warnings= | 収集済み警告をクリア |
| =core-native-warnings-count= | 警告数を返す |
| =core-native-show-warnings= | 警告を専用バッファに表示 |
| =core-native-save-warnings= | 警告をファイルに保存 |
| =core-native-set-mode= | レポートモード切替（silent/collect/verbose） |

** Org Agenda キャッシュ API

| 関数 | 用途 |
|------|------|
| =utils-org-agenda-build= | Org ファイルリストをスキャン＆キャッシュして返す |
| =utils-org-agenda-invalidate-cache= | キャッシュを強制無効化 |

** Org フォールド API

| 関数 | 用途 |
|------|------|
| =my/org-fold-subtree= | 現在位置のサブツリーを折り畳む |
| =my/org-unfold-subtree= | 現在位置のサブツリーを展開する |
| =my/org-toggle-fold= | 折り畳み状態をトグル |

** 診断・デバッグ API

| 関数 | 用途 |
|------|------|
| =my/print-load-path= | =load-path= を専用バッファに表示 |
| =my/print-feature-list= | 読み込み済みフィーチャ一覧を表示 |
| =my/check-missing-executables= | 外部ツール（rg, git, make 等）の不在チェック |
| =my/toggle-debug-on-error= | =debug-on-error= のトグル |
| =my/print-build-info= | Emacs ビルド情報を Markdown テーブルで表示 |


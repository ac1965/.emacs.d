# Emacs のバックアップ / ロックファイル
*~
\#*

# no-littering の状態ディレクトリ（no-littering-var-directory 等）
.cache
.etc
.var

# README.org から `make tangle` / `make reload` で生成されるファイル一式。
# ソース・オブ・トゥルースは README.org のみで、これらは常に再生成できる。
early-init.el
init.el
user.el
lisp
personal
scripts
Makefile

# design_spec.org / README.org の Appendix から `make diagrams`
#（= `make dot-tangle dot-svg mmd-tangle mmd-svg`）で生成される図版一式。
# design_spec.org 由来（01_*.dot 〜 13_*.dot、命名規則: 2桁連番プレフィックス）
svg/[0-9][0-9]_*.dot
# README.org 由来
svg/tangle-flow.mmd
svg/emacs_config_layer_architecture.dot
svg/emacs_keybindings_cheatsheet_concept.dot

.tangle
.tangle-stamp
.aider.*
.aider*
.markdown-preview.html
.org-id-locations
tree-sitter
eln-cache
eshell
bookmarks.eld
history
emms
abbrev_defs
org-agenda-files.cache

# native-comp が eln-cache の外に漏らすことがある stray *.eln（Makefile の
# distclean ターゲットのコメント参照）
*.eln

# macOS
.DS_Store

.env.el
.mypy_cache
*.patch
*.pdf
*.tex
*.synctex.gz
*.aux
*.out
*.log
*.toc
*.fdb_latexmk
*.fls
/diffs.jsonl
/messages.json
/a
/straight/
.claude
